import 'package:flutter/material.dart';

void main()=>runApp(Xylophone_class());

class Xylophone_class extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
      home: Scaffold(
        backgroundColor: Colors.blue,
        body: SafeArea(child:
        Container())
      ),
    );
  }

}
